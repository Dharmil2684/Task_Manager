namespace TaskManager.Core.Entities
{
    public enum UserRole
    {
        Employee,
        Manager,
        TenantAdmin
    }
    public enum WorkTaskStatus
    {
        Pending,
        InProgress,
        Completed
    }
}
