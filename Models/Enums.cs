namespace TaskManager.Core.Enums
{
    public enum UserRole
    {
        TenantAdmin,
        Manager,
        Employee
    }

    public enum WorkTaskStatus
    {
        Pending,
        InProgress,
        Completed
    }
}
