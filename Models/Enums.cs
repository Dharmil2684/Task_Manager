namespace TaskManager.Models
{
    public enum UserRole
    {
        TenantAdmin,
        Manager,
        Employee
    }
    public enum WorkTaskStatus
    {
        Pending,
        InProgress,
        Completed
    }
}
